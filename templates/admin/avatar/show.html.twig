{% extends 'base_admin.html.twig' %}

{% block title %}{{ avatar.name }} - Admin{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1>👤 {{ avatar.name }}</h1>
    <p class="text-muted">Avatar de {{ avatar.user.username }}</p>
    
    <div class="section-divider"></div>

    <table class="table">
        <tbody>
            <tr>
                <th style="width: 200px;">ID</th>
                <td>{{ avatar.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ avatar.name }}</td>
            </tr>
            <tr>
                <th>Utilisateur</th>
                <td>{{ avatar.user.username }}</td>
            </tr>
            <tr>
                <th>Créé le</th>
                <td>{{ avatar.createdAt|date('d/m/Y H:i:s') }}</td>
            </tr>
            <tr>
                <th>Modifié le</th>
                <td>{{ avatar.updatedAt|date('d/m/Y H:i:s') }}</td>
            </tr>
        </tbody>
    </table>

    {% if avatar.avatarSkills|length > 0 %}
    <h3 class="mt-4">📊 Compétences ({{ avatar.avatarSkills|length }})</h3>
    <div class="row">
        {% for avatarSkill in avatar.avatarSkills|sort((a, b) => b.level <=> a.level) %}
        <div class="col-md-6 col-lg-4 mb-3">
            <div class="card">
                <div class="card-body">
                    <h6 class="card-title">{{ avatarSkill.skill.name }}</h6>
                    <p class="mb-1">
                        <span class="badge bg-{% if avatarSkill.skill.uiGroup == 'combat' %}danger{% else %}primary{% endif %}">
                            {{ avatarSkill.skill.uiGroup }}
                        </span>
                    </p>
                    <div class="progress" style="height: 25px;">
                        <div class="progress-bar bg-success" role="progressbar" 
                             style="width: {{ (avatarSkill.level / avatarSkill.skill.skillLevelCap * 100)|round }}%"
                             aria-valuenow="{{ avatarSkill.level }}" 
                             aria-valuemin="0" 
                             aria-valuemax="{{ avatarSkill.skill.skillLevelCap }}">
                            {{ avatarSkill.level }} / {{ avatarSkill.skill.skillLevelCap }}
                        </div>
                    </div>
                </div>
            </div>
        </div>
        {% endfor %}
    </div>
    {% else %}
    <div class="alert alert-info mt-4">
        Aucune compétence définie pour cet avatar.
    </div>
    {% endif %}

    <div class="mb-3 mt-4">
        <a href="{{ path('admin_avatar_edit', {'id': avatar.id}) }}" class="btn btn-warning">Éditer</a>
        <a href="{{ path('admin_avatar_index') }}" class="btn btn-secondary">Retour à la liste</a>
    </div>

    <form method="post" action="{{ path('admin_avatar_delete', {'id': avatar.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet avatar ?');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ avatar.id) }}">
        <button class="btn btn-danger">Supprimer</button>
    </form>
</div>
{% endblock %}
