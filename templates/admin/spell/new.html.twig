{% extends 'base_admin.html.twig' %}

{% block title %}Nouveau Sort{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1>➕ Nouveau Sort</h1>
    <p class="text-muted">Créez un nouveau sort</p>
    
    <div class="section-divider"></div>

    <div class="card">
        <div class="card-body">
            {{ form_start(form) }}
                {{ form_widget(form) }}
                <div class="mt-4 d-flex gap-2">
                    <button type="submit" class="btn btn-success">💾 Créer</button>
                    <a href="{{ path('admin_spell_index') }}" class="btn btn-secondary">❌ Annuler</a>
                </div>
            {{ form_end(form) }}
        </div>
    </div>
</div>
{% endblock %}
