{% extends 'base_admin.html.twig' %}

{% block title %}Éditer: {{ spell.name }}{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1>✏️ Éditer: {{ spell.name }}</h1>
    <p class="text-muted">Modifiez les informations du sort</p>
    
    <div class="section-divider"></div>

    <div class="card">
        <div class="card-body">
            {{ form_start(form) }}
                {{ form_widget(form) }}
                <div class="mt-4 d-flex gap-2">
                    <button type="submit" class="btn btn-success">💾 Enregistrer</button>
                    <a href="{{ path('admin_spell_index') }}" class="btn btn-secondary">❌ Annuler</a>
                </div>
            {{ form_end(form) }}
        </div>
    </div>

    <hr class="mt-4">

    <form method="post" action="{{ path('admin_spell_delete', {'id': spell.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce sort ?');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ spell.id) }}">
        <button class="btn btn-danger">Supprimer ce sort</button>
    </form>
</div>
{% endblock %}
