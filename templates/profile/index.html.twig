{% extends 'base.html.twig' %}

{% block title %}Mon Profil - Pax Dei Market{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1>👤 Mon Profil</h1>
    <p class="text-muted">Gérez vos préférences et paramètres</p>
    
    <div class="section-divider"></div>

    {% for message in app.flashes('success') %}
        <div class="alert alert-success alert-dismissible fade show">
            {{ message }}
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    {% endfor %}

    <div class="row">
        <div class="col-md-4 mb-4">
            <div class="card">
                <div class="card-body text-center">
                    <div class="mb-3">
                        <div style="width: 100px; height: 100px; background: linear-gradient(135deg, var(--primary) 0%, var(--primary-dark) 100%); border-radius: 50%; display: flex; align-items: center; justify-content: center; margin: 0 auto; color: white; font-size: 2.5rem;">
                            {{ user.username|first|upper }}
                        </div>
                    </div>
                    <h3>{{ user.username }}</h3>
                    <p class="text-muted">
                        {% if 'ROLE_ADMIN' in user.roles %}
                            <span class="badge bg-danger">Administrateur</span>
                        {% else %}
                            <span class="badge bg-primary">Utilisateur</span>
                        {% endif %}
                    </p>
                </div>
            </div>

            <div class="card mt-3">
                <div class="card-header">
                    <h5 class="mb-0">⚙️ Paramètres</h5>
                </div>
                <div class="card-body">
                    <div class="mb-3">
                        <strong>Nom d'utilisateur :</strong> {{ user.username }}
                    </div>
                    <div class="mb-3">
                        <strong>Rôle :</strong> 
                        {% if 'ROLE_ADMIN' in user.roles %}
                            Administrateur
                        {% else %}
                            Utilisateur
                        {% endif %}
                    </div>
                    <a href="{{ path('app_logout') }}" class="btn btn-danger">
                        🚪 Se déconnecter
                    </a>
                </div>
            </div>
        </div>

        <div class="col-md-8 mb-4">
            <div class="card">
                <div class="card-header d-flex justify-content-between align-items-center">
                    <h3 class="mb-0">📋 Mes vues personnalisées</h3>
                    <a href="{{ path('app_profile_view_new') }}" class="btn btn-primary">
                        ➕ Nouvelle vue
                    </a>
                </div>
                <div class="card-body">
                    {% if user.userViews|length > 0 %}
                        <div class="list-group">
                            {% for view in user.userViews %}
                                <div class="list-group-item">
                                    <div class="d-flex justify-content-between align-items-start">
                                        <div>
                                            <h5 class="mb-1">
                                                <a href="{{ path('app_view_show', {id: view.id}) }}" class="text-decoration-none">
                                                    {{ view.name }}
                                                </a>
                                            </h5>
                                            <p class="mb-1 text-muted">
                                                <small>
                                                    📁 {{ view.categories|length }} catégorie(s) • 
                                                    📦 {{ view.items|length }} item(s) spécifique(s)
                                                </small>
                                            </p>
                                        </div>
                                        <div class="btn-group btn-group-sm">
                                            <a href="{{ path('app_view_show', {id: view.id}) }}" class="btn btn-outline-primary" title="Voir">
                                                👁️
                                            </a>
                                            <a href="{{ path('app_profile_view_edit', {id: view.id}) }}" class="btn btn-outline-secondary" title="Modifier">
                                                ✏️
                                            </a>
                                            <form action="{{ path('app_profile_view_delete', {id: view.id}) }}" method="post" class="d-inline" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette vue ?');">
                                                <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ view.id) }}">
                                                <button type="submit" class="btn btn-outline-danger" title="Supprimer">
                                                    🗑️
                                                </button>
                                            </form>
                                        </div>
                                    </div>
                                </div>
                            {% endfor %}
                        </div>
                    {% else %}
                        <div class="text-center py-5">
                            <p class="text-muted mb-3">
                                Vous n'avez pas encore créé de vue personnalisée.
                            </p>
                            <p class="text-muted mb-3">
                                Les vues personnalisées vous permettent de filtrer et suivre facilement vos catégories et items favoris.
                            </p>
                            <a href="{{ path('app_profile_view_new') }}" class="btn btn-primary">
                                Créer ma première vue
                            </a>
                        </div>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
